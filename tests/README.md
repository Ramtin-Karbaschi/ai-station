# Tool-calling and JSON contract smoke tests for llama.cpp

These tests are live/optional. Unit-style offline checks validate catalog flags.

Run live (requires healthy general server):

~~~bash
python3 tests/test_openai_contracts.py --live
~~~
