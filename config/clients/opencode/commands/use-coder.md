---
description: Warm the default Coder profile without hiding picker models
agent: station-ops
---
Run `ai opencode use coder`. It aligns the GPU profile with `model` and `small_model` while keeping every model visible in the picker. Report the result briefly and stop.
